{
  "name": "log-vpn",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "main": "dist_electron/main.js",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server/_core/index.ts",
    "build:renderer": "vite build",
    "build:main": "esbuild client-src/main.ts --bundle --platform=node --outfile=dist_electron/main.js",
    "build": "pnpm run build:renderer && pnpm run build:main && electron-builder"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3",
    "esbuild": "^0.20.2",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
